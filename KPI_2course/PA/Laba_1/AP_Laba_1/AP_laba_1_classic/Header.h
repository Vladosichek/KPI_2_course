#pragma once
#include <iostream>
#include <ctime>
#include <fstream>
#include <string>
#include <cmath>
using namespace std;
void gen(char*, int);
int coun(int);
void outcontent(char*);
void merge(char*, int);