#pragma once
#include <ctime>
#include <iostream>
#include <fstream>
#include <string>
#include <cmath>
using namespace std;
void merge(int*, int, int, int);
void mergeSort(int*, int, int);
void gen(int, int, char*);
void GenSort(int, int, char*);